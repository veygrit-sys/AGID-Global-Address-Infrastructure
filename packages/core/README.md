# @agid/core

AGID encoding and decoding, 21-bit grid geometry, adjacent-cell matching, and
public building-reference primitives.

The root modules remain authoritative during migration. This bridge preserves
their existing exports while package boundaries and generated SDK compatibility
are verified.
