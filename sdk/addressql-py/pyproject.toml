[project]
name = "addressql"
version = "0.4.0"
description = "Local-first AddressQL SDK for country, postal, normalization, matching, distance, and delivery decisions."
requires-python = ">=3.10"
license = "Apache-2.0"

[tool.pytest.ini_options]
pythonpath = ["."]
