[package]
name = "addressql-sdk"
version = "0.4.0"
edition = "2021"
license = "Apache-2.0"
description = "Rust SDK facade for AddressQL core functions."

[dependencies]
addressql-core = { path = "../../native/addressql-core" }
